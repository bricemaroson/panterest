{% extends 'base.html.twig' %}

{% block title %} {{ pin.title }} {% endblock %}

{% block body %}
    <article>
        <h1>{{pin.title}}</h1>
        <p>{{pin.description}}</p>
        <p>{{pin.createdAt}}</p>
    </article>
    <a href ="{{ path('app_home') }}">Go Back</a>
{% endblock %}
